import 'package:demo/presentation/theme/theme_config.dart';
import 'package:flutter/material.dart';

class AppTextStyle {
  TextStyle get style08 => TextStyle(
      fontSize: ThemeConfig.dimens.font08,
      fontFamily: ThemeConfig.strings.fontName,
      color: ThemeConfig.colors.primary);

  TextStyle get style09 => TextStyle(
      fontSize: ThemeConfig.dimens.font09,
      fontFamily: ThemeConfig.strings.fontName,
      color: ThemeConfig.colors.primary);

  TextStyle get style10 => TextStyle(
      fontSize: ThemeConfig.dimens.font10,
      fontFamily: ThemeConfig.strings.fontName,
      color: ThemeConfig.colors.primary);

  TextStyle get style12 => TextStyle(
      fontSize: ThemeConfig.dimens.font12,
      fontFamily: ThemeConfig.strings.fontName,
      color: ThemeConfig.colors.primary);
  TextStyle get style13 => TextStyle(
      fontSize: ThemeConfig.dimens.font13,
      fontFamily: ThemeConfig.strings.fontName,
      color: ThemeConfig.colors.primary);
  TextStyle get style14 => TextStyle(
      fontSize: ThemeConfig.dimens.font14,
      fontFamily: ThemeConfig.strings.fontName,
      color: ThemeConfig.colors.primary);

  TextStyle get style16 => TextStyle(
      fontSize: ThemeConfig.dimens.font16,
      fontFamily: ThemeConfig.strings.fontName,
      color: ThemeConfig.colors.primary);

  TextStyle get style18 => TextStyle(
      fontSize: ThemeConfig.dimens.font18,
      fontFamily: ThemeConfig.strings.fontName,
      color: ThemeConfig.colors.primary);

  TextStyle get style20 => TextStyle(
      fontSize: ThemeConfig.dimens.font20,
      fontFamily: ThemeConfig.strings.fontName,
      color: ThemeConfig.colors.primary);

  TextStyle get style22 => TextStyle(
      fontSize: ThemeConfig.dimens.font22,
      fontFamily: ThemeConfig.strings.fontName,
      color: ThemeConfig.colors.primary);

  TextStyle get style24 => TextStyle(
      fontSize: ThemeConfig.dimens.font24,
      fontFamily: ThemeConfig.strings.fontName,
      color: ThemeConfig.colors.primary);

  TextStyle get style26 => TextStyle(
      fontSize: ThemeConfig.dimens.font26,
      fontFamily: ThemeConfig.strings.fontName,
      color: ThemeConfig.colors.primary);

  TextStyle get style28 => TextStyle(
      fontSize: ThemeConfig.dimens.font28,
      fontFamily: ThemeConfig.strings.fontName,
      color: ThemeConfig.colors.primary);

  TextStyle get styleHint14 => TextStyle(
      fontSize: ThemeConfig.dimens.font14,
      fontFamily: ThemeConfig.strings.fontName,
      color: ThemeConfig.colors.hintColor);
}
